# Sign Language Reader - Requirements for Building EXE

# Core dependencies (already in built-in/requirements.txt)
opencv-python>=4.8.0
mediapipe>=0.10.0
pillow>=10.0.0
scikit-learn>=1.3.0
numpy>=1.24.0
paho-mqtt>=1.6.0

# Build tool for creating .exe
pyinstaller>=6.0.0

# Optional: for better EXE compression
upx-windows-binaries>=4.0.0; sys_platform == 'win32'
